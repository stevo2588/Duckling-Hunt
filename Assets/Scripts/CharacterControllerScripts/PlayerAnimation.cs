using UnityEngine;
using System;

public class PlayerAnimation: MonoBehaviour {
	/*
	public float runSpeedScale = 1.0f;
	public float walkSpeedScale = 1.0f;
	
	public void Start (){
		
		
		// By default loop all animations
		animation.wrapMode = WrapMode.Loop;
	
		animation["waddle"].layer = -1;
		animation.SyncLayer(-1);
	
		// We are in full control here - don't let any other animations play when we start
		animation.Stop();
	}
	
	public void Update (){
		float currentSpeed = characterMainGame.getSpeed();
	
		if (currentSpeed > 0.1){ // Fade in walk
			animation.CrossFade("walk");
		}
		animation["walk"].normalizedSpeed = walkSpeedScale;

	}*/
}